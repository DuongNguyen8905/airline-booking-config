server:
  port: 5001

spring:
  datasource:
    url: jdbc:mysql://userdb:3306/airline_user
    username: root
    password: root
 

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
